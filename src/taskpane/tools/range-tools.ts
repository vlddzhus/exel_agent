/**
 * range-tools.ts — ПУСТОЙ файл-маркер.
 *
 * Все инструменты из этого файла мигрированы в Неделю 3 Фазы 1:
 *   - getSelectedRange, getRange    → R2 getRangeTool (read.ts)
 *   - getWorksheetInfo, getAllData  → R1 getWorkbookOverviewTool (read.ts)
 *   - setValues                     → W1 setValuesTool (write.ts)
 *   - clearRange                    → W5 clearRangeTool (write.ts)
 *
 * См. docs/03-TOOLS-SPEC.md §1 (реестр 24 инструментов), §2 W1/W2.
 *
 * Файл оставлен пустым (не удалён) чтобы не ломать существующие импорты, если
 * где-то остался side-effect `import "./tools/range-tools"`. В новой сессии
 * при желании можно удалить файл целиком + вычистить импорт из taskpane.ts.
 */
export {};
